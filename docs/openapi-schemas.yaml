# AUTO-GENERATED by project/bin/gen_table_catalog.py. Do not edit.
# Source: project/bin/build_sqlite.py (SCHEMAS dict)


components:
  schemas:
    IncidentRow:
      type: object
      description: 'Slim/indexed row shape from the `incident` table (task, hr-gated). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope. Directly gated: rows where `assignment_group` is the HR group are hidden when locked.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        number:
          type: string
        short_description:
          type: string
        state:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        priority:
          type: string
        impact:
          type: string
        urgency:
          type: string
        assigned_to:
          type: string
        assignment_group:
          type: string
        cmdb_ci:
          type: string
        caller_id:
          type: string
        contact_type:
          type: string
        opened_at:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
        legal_hold:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        sys_class_name:
          type: string
        parent:
          type: string
        category:
          type: string
        subcategory:
          type: string
      required: [sys_id]
      additionalProperties: true
    ChangeRequestRow:
      type: object
      description: 'Slim/indexed row shape from the `change_request` table (task, cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        number:
          type: string
        short_description:
          type: string
        state:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        priority:
          type: string
        impact:
          type: string
        urgency:
          type: string
        assigned_to:
          type: string
        assignment_group:
          type: string
        cmdb_ci:
          type: string
        caller_id:
          type: string
        contact_type:
          type: string
        opened_at:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
        legal_hold:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        sys_class_name:
          type: string
        parent:
          type: string
        category:
          type: string
        std_change_producer_version:
          type: string
        chg_model:
          type: string
        type:
          type: string
      required: [sys_id]
      additionalProperties: true
    ProblemRow:
      type: object
      description: 'Slim/indexed row shape from the `problem` table (task, cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        number:
          type: string
        short_description:
          type: string
        state:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        priority:
          type: string
        impact:
          type: string
        urgency:
          type: string
        assigned_to:
          type: string
        assignment_group:
          type: string
        cmdb_ci:
          type: string
        caller_id:
          type: string
        contact_type:
          type: string
        opened_at:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
        legal_hold:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        sys_class_name:
          type: string
        parent:
          type: string
        category:
          type: string
        subcategory:
          type: string
      required: [sys_id]
      additionalProperties: true
    ProblemTaskRow:
      type: object
      description: 'Slim/indexed row shape from the `problem_task` table (task, cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        number:
          type: string
        short_description:
          type: string
        state:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        priority:
          type: string
        impact:
          type: string
        urgency:
          type: string
        assigned_to:
          type: string
        assignment_group:
          type: string
        cmdb_ci:
          type: string
        caller_id:
          type: string
        contact_type:
          type: string
        opened_at:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
        legal_hold:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        sys_class_name:
          type: string
        parent:
          type: string
        problem:
          type: string
      required: [sys_id]
      additionalProperties: true
    ScRequestRow:
      type: object
      description: 'Slim/indexed row shape from the `sc_request` table (task, cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        number:
          type: string
        short_description:
          type: string
        state:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        priority:
          type: string
        impact:
          type: string
        urgency:
          type: string
        assigned_to:
          type: string
        assignment_group:
          type: string
        cmdb_ci:
          type: string
        caller_id:
          type: string
        contact_type:
          type: string
        opened_at:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
        legal_hold:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        sys_class_name:
          type: string
        parent:
          type: string
        requested_for:
          type: string
      required: [sys_id]
      additionalProperties: true
    ScReqItemRow:
      type: object
      description: 'Slim/indexed row shape from the `sc_req_item` table (task, cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        number:
          type: string
        short_description:
          type: string
        state:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        priority:
          type: string
        impact:
          type: string
        urgency:
          type: string
        assigned_to:
          type: string
        assignment_group:
          type: string
        cmdb_ci:
          type: string
        caller_id:
          type: string
        contact_type:
          type: string
        opened_at:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
        legal_hold:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        sys_class_name:
          type: string
        parent:
          type: string
        requested_for:
          type: string
        request:
          type: string
        cat_item:
          type: string
        configuration_item:
          type: string
      required: [sys_id]
      additionalProperties: true
    ScTaskRow:
      type: object
      description: 'Slim/indexed row shape from the `sc_task` table (task, cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        number:
          type: string
        short_description:
          type: string
        state:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        priority:
          type: string
        impact:
          type: string
        urgency:
          type: string
        assigned_to:
          type: string
        assignment_group:
          type: string
        cmdb_ci:
          type: string
        caller_id:
          type: string
        contact_type:
          type: string
        opened_at:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
        legal_hold:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        sys_class_name:
          type: string
        parent:
          type: string
        requested_for:
          type: string
        request_item:
          type: string
        request:
          type: string
      required: [sys_id]
      additionalProperties: true
    IncidentTaskRow:
      type: object
      description: 'Slim/indexed row shape from the `incident_task` table (task, cache-5min, hr-gated). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope. Parent-gated: rows whose `incident` references an HR-assigned incident are hidden when locked.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        number:
          type: string
        short_description:
          type: string
        state:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        priority:
          type: string
        impact:
          type: string
        urgency:
          type: string
        assigned_to:
          type: string
        assignment_group:
          type: string
        cmdb_ci:
          type: string
        caller_id:
          type: string
        contact_type:
          type: string
        opened_at:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
        legal_hold:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        sys_class_name:
          type: string
        parent:
          type: string
        incident:
          type: string
      required: [sys_id]
      additionalProperties: true
    ChangeTaskRow:
      type: object
      description: 'Slim/indexed row shape from the `change_task` table (task, cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        number:
          type: string
        short_description:
          type: string
        state:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        priority:
          type: string
        impact:
          type: string
        urgency:
          type: string
        assigned_to:
          type: string
        assignment_group:
          type: string
        cmdb_ci:
          type: string
        caller_id:
          type: string
        contact_type:
          type: string
        opened_at:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
        legal_hold:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        sys_class_name:
          type: string
        parent:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysapprovalGroupRow:
      type: object
      description: 'Slim/indexed row shape from the `sysapproval_group` table (task, cache-5min, hr-gated). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope. Parent-gated: rows whose `parent` references an HR-assigned incident are hidden when locked.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        number:
          type: string
        short_description:
          type: string
        state:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        priority:
          type: string
        impact:
          type: string
        urgency:
          type: string
        assigned_to:
          type: string
        assignment_group:
          type: string
        cmdb_ci:
          type: string
        caller_id:
          type: string
        contact_type:
          type: string
        opened_at:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
        legal_hold:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        sys_class_name:
          type: string
        parent:
          type: string
        approval:
          type: string
        approval_user:
          type: string
      required: [sys_id]
      additionalProperties: true
    AssetTaskRow:
      type: object
      description: 'Slim/indexed row shape from the `asset_task` table (task, cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        number:
          type: string
        short_description:
          type: string
        state:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        priority:
          type: string
        impact:
          type: string
        urgency:
          type: string
        assigned_to:
          type: string
        assignment_group:
          type: string
        cmdb_ci:
          type: string
        caller_id:
          type: string
        contact_type:
          type: string
        opened_at:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
        legal_hold:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        sys_class_name:
          type: string
        parent:
          type: string
      required: [sys_id]
      additionalProperties: true
    SnContractRenewalTaskRow:
      type: object
      description: 'Slim/indexed row shape from the `sn_contract_renewal_task` table (task, cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        number:
          type: string
        short_description:
          type: string
        state:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        priority:
          type: string
        impact:
          type: string
        urgency:
          type: string
        assigned_to:
          type: string
        assignment_group:
          type: string
        cmdb_ci:
          type: string
        caller_id:
          type: string
        contact_type:
          type: string
        opened_at:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
        legal_hold:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        sys_class_name:
          type: string
        parent:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysUserRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_user` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        user_name:
          type: string
        name:
          type: string
        email:
          type: string
        title:
          type: string
        department:
          type: string
        location:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    SysUserGroupRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_user_group` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        manager:
          type: string
        description:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    SysUserGrmemberRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_user_grmember` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        group:
          type: string
        user:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysUserDelegateRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_user_delegate` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        user:
          type: string
        delegate:
          type: string
        starts:
          type: string
        ends:
          type: string
        approvals:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        assignments:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        notifications:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        invitations:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    SysUserHasRoleRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_user_has_role` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        user:
          type: string
        role:
          type: string
        state:
          type: string
        granted_by:
          type: string
        inherited:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    SysUserRoleRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_user_role` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        description:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysGroupHasRoleRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_group_has_role` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        group:
          type: string
        role:
          type: string
        granted_by:
          type: string
        inherits:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    KbKnowledgeRow:
      type: object
      description: 'Slim/indexed row shape from the `kb_knowledge` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        number:
          type: string
        short_description:
          type: string
        kb_knowledge_base:
          type: string
        kb_category:
          type: string
        workflow_state:
          type: string
        author:
          type: string
        article_type:
          type: string
        valid_to:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    CmdbCiRow:
      type: object
      description: 'Slim/indexed row shape from the `cmdb_ci` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        sys_class_name:
          type: string
        operational_status:
          type: string
        owned_by:
          type: string
        short_description:
          type: string
        install_status:
          type: string
        discovery_source:
          type: string
        last_discovered:
          type: string
        support_group:
          type: string
        category:
          type: string
        ip_address:
          type: string
        fqdn:
          type: string
      required: [sys_id]
      additionalProperties: true
    CmdbRelCiRow:
      type: object
      description: 'Slim/indexed row shape from the `cmdb_rel_ci` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        parent:
          type: string
        child:
          type: string
        type:
          type: string
          description: Display value (label form).
      required: [sys_id]
      additionalProperties: true
    SysChoiceRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_choice` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        table:
          type: string
        element:
          type: string
        value:
          type: string
        label:
          type: string
      required: [sys_id]
      additionalProperties: true
    CoreCompanyRow:
      type: object
      description: 'Slim/indexed row shape from the `core_company` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        short:
          type: string
      required: [sys_id]
      additionalProperties: true
    CmnDepartmentRow:
      type: object
      description: 'Slim/indexed row shape from the `cmn_department` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        cost_center:
          type: string
      required: [sys_id]
      additionalProperties: true
    CmnLocationRow:
      type: object
      description: 'Slim/indexed row shape from the `cmn_location` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        city:
          type: string
        state:
          type: string
      required: [sys_id]
      additionalProperties: true
    CmnCostCenterRow:
      type: object
      description: 'Slim/indexed row shape from the `cmn_cost_center` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        code:
          type: string
        name:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysJournalFieldRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_journal_field` table (hr-gated). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope. Parent-gated: rows whose `element_id` references an HR-assigned incident are hidden when locked.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        element_id:
          type: string
        element:
          type: string
        value:
          type: string
        sys_created_by:
          type: string
        sys_created_on:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysAuditRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_audit` table (hr-gated). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope. Parent-gated: rows whose `documentkey` references an HR-assigned incident are hidden when locked.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        tablename:
          type: string
        documentkey:
          type: string
        fieldname:
          type: string
        fieldlabel:
          type: string
        oldvalue:
          type: string
        newvalue:
          type: string
        user:
          type: string
        sys_created_on:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysAttachmentRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_attachment` table (hr-gated). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope. Parent-gated: rows whose `table_sys_id` references an HR-assigned incident are hidden when locked.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        table_name:
          type: string
        table_sys_id:
          type: string
        file_name:
          type: string
        content_type:
          type: string
        size_bytes:
          type: string
        sys_created_by:
          type: string
        sys_created_on:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysEmailRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_email` table (hr-gated). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope. Parent-gated: rows whose `instance` references an HR-assigned incident are hidden when locked.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        instance:
          type: string
        target_table:
          type: string
        type:
          type: string
        mailbox:
          type: string
        state:
          type: string
        subject:
          type: string
        recipients:
          type: string
        user:
          type: string
        sys_created_on:
          type: string
      required: [sys_id]
      additionalProperties: true
    TaskCiRow:
      type: object
      description: 'Slim/indexed row shape from the `task_ci` table (cache-5min, hr-gated). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope. Parent-gated: rows whose `task` references an HR-assigned incident are hidden when locked.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        task:
          type: string
        ci:
          type: string
      required: [sys_id]
      additionalProperties: true
    TaskSlaRow:
      type: object
      description: 'Slim/indexed row shape from the `task_sla` table (cache-5min, hr-gated). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope. Parent-gated: rows whose `task` references an HR-assigned incident are hidden when locked.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        task:
          type: string
        sla_definition:
          type: string
          description: Display value (label form).
        stage:
          type: string
        business_percentage:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysapprovalApproverRow:
      type: object
      description: 'Slim/indexed row shape from the `sysapproval_approver` table (cache-5min, hr-gated). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope. Parent-gated: rows whose `sysapproval` references an HR-assigned incident are hidden when locked.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        sysapproval:
          type: string
        group:
          type: string
        approver:
          type: string
        state:
          type: string
        sys_created_on:
          type: string
      required: [sys_id]
      additionalProperties: true
    ScCatItemRow:
      type: object
      description: 'Slim/indexed row shape from the `sc_cat_item` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        short_description:
          type: string
        sys_class_name:
          type: string
        sc_catalogs:
          type: string
      required: [sys_id]
      additionalProperties: true
    ItemOptionNewRow:
      type: object
      description: 'Slim/indexed row shape from the `item_option_new` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        question_text:
          type: string
        type:
          type: string
        cat_item:
          type: string
        mandatory:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        order:
          type: string
        reference:
          type: string
      required: [sys_id]
      additionalProperties: true
    ScItemOptionRow:
      type: object
      description: 'Slim/indexed row shape from the `sc_item_option` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        value:
          type: string
        item_option_new:
          type: string
        cat_item:
          type: string
        order:
          type: string
      required: [sys_id]
      additionalProperties: true
    ScItemOptionMtomRow:
      type: object
      description: 'Slim/indexed row shape from the `sc_item_option_mtom` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        request_item:
          type: string
        sc_item_option:
          type: string
      required: [sys_id]
      additionalProperties: true
    QuestionRow:
      type: object
      description: 'Slim/indexed row shape from the `question` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        question_text:
          type: string
        type:
          type: string
      required: [sys_id]
      additionalProperties: true
    QuestionChoiceRow:
      type: object
      description: 'Slim/indexed row shape from the `question_choice` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        question:
          type: string
        text:
          type: string
        value:
          type: string
        order:
          type: string
      required: [sys_id]
      additionalProperties: true
    ScCatalogRow:
      type: object
      description: 'Slim/indexed row shape from the `sc_catalog` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        title:
          type: string
        description:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    ScCategoryRow:
      type: object
      description: 'Slim/indexed row shape from the `sc_category` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        title:
          type: string
        description:
          type: string
        sc_catalog:
          type: string
        parent:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    CatalogUiPolicyRow:
      type: object
      description: 'Slim/indexed row shape from the `catalog_ui_policy` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        cat_item:
          type: string
        short_description:
          type: string
        on_load:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        reverse_if_false:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        applies_to:
          type: string
        order:
          type: string
      required: [sys_id]
      additionalProperties: true
    CatalogUiPolicyActionRow:
      type: object
      description: 'Slim/indexed row shape from the `catalog_ui_policy_action` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        ui_policy:
          type: string
        cat_item:
          type: string
        variable:
          type: string
        mandatory:
          type: string
        visible:
          type: string
        disabled:
          type: string
        cleared:
          type: string
      required: [sys_id]
      additionalProperties: true
    CatalogScriptClientRow:
      type: object
      description: 'Slim/indexed row shape from the `catalog_script_client` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        cat_item:
          type: string
        name:
          type: string
        type:
          type: string
        cat_variable:
          type: string
        ui_type:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        applies_to:
          type: string
      required: [sys_id]
      additionalProperties: true
    UserCriteriaRow:
      type: object
      description: 'Slim/indexed row shape from the `user_criteria` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        description:
          type: string
        advanced:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    ScCatItemUserCriteriaMtomRow:
      type: object
      description: 'Slim/indexed row shape from the `sc_cat_item_user_criteria_mtom` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        sc_cat_item:
          type: string
        user_criteria:
          type: string
      required: [sys_id]
      additionalProperties: true
    ScCatItemUserCriteriaNoMtomRow:
      type: object
      description: 'Slim/indexed row shape from the `sc_cat_item_user_criteria_no_mtom` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        sc_cat_item:
          type: string
        user_criteria:
          type: string
      required: [sys_id]
      additionalProperties: true
    ItemOptionNewSetRow:
      type: object
      description: 'Slim/indexed row shape from the `item_option_new_set` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        internal_name:
          type: string
        title:
          type: string
        description:
          type: string
        layout:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    IoSetItemRow:
      type: object
      description: 'Slim/indexed row shape from the `io_set_item` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        sc_cat_item:
          type: string
        variable_set:
          type: string
        order:
          type: string
      required: [sys_id]
      additionalProperties: true
    TopicRow:
      type: object
      description: 'Slim/indexed row shape from the `topic` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        description:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    StdChangeProposalRow:
      type: object
      description: 'Slim/indexed row shape from the `std_change_proposal` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        number:
          type: string
        template_name:
          type: string
        short_description:
          type: string
        proposal_type:
          type: string
        state:
          type: string
        catalog:
          type: string
        category:
          type: string
        opened_at:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysScriptRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_script` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        collection:
          type: string
        when:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        priority:
          type: string
        condition:
          type: string
        filter_condition:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysScriptClientRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_script_client` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        table:
          type: string
        type:
          type: string
        ui_type:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        condition:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysScriptIncludeRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_script_include` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        api_name:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        description:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysautoScriptRow:
      type: object
      description: 'Slim/indexed row shape from the `sysauto_script` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        run_type:
          type: string
        run_period:
          type: string
        run_time:
          type: string
        run_dayofweek:
          type: string
        run_dayofmonth:
          type: string
        conditional:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        condition:
          type: string
        run_as:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysUiPolicyRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_ui_policy` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        short_description:
          type: string
        table:
          type: string
        model_table:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        conditions:
          type: string
        ui_type:
          type: string
        run_scripts:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    SysUiPolicyActionRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_ui_policy_action` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        ui_policy:
          type: string
        table:
          type: string
        field:
          type: string
        mandatory:
          type: string
        visible:
          type: string
        disabled:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysDataPolicy2Row:
      type: object
      description: 'Slim/indexed row shape from the `sys_data_policy2` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        short_description:
          type: string
        model_table:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        conditions:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysDataPolicyRuleRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_data_policy_rule` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        sys_data_policy:
          type: string
        table:
          type: string
        field:
          type: string
        mandatory:
          type: string
        disabled:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysPropertiesRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_properties` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        value:
          type: string
        type:
          type: string
          description: Display value (label form).
        description:
          type: string
        sys_class_name:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysUiActionRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_ui_action` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        action_name:
          type: string
        table:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        condition:
          type: string
        client:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        form_button:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        list_button:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        order:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysDictionaryRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_dictionary` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        element:
          type: string
        internal_type:
          type: string
          description: Display value (label form).
        column_label:
          type: string
        mandatory:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        default_value:
          type: string
        reference:
          type: string
        read_only:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    SysDictionaryOverrideRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_dictionary_override` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        element:
          type: string
        base_table:
          type: string
        mandatory_override:
          type: string
        read_only_override:
          type: string
        default_value_override:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysHubFlowRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_hub_flow` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        internal_name:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        type:
          type: string
          description: Display value (label form).
        sys_class_name:
          type: string
        description:
          type: string
      required: [sys_id]
      additionalProperties: true
    FlowInventoryRow:
      type: object
      description: 'Slim/indexed row shape from the `flow_inventory` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        internal_name:
          type: string
        area:
          type: string
        category:
          type: string
        pattern:
          type: string
        catalog_item:
          type: string
        flow_type:
          type: string
        trigger_table:
          type: string
        run_count:
          type: string
        error_count:
          type: string
        last_run:
          type: string
        created_by:
          type: string
        curated:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        sys_updated_on:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysHubActionInstanceV2Row:
      type: object
      description: 'Slim/indexed row shape from the `sys_hub_action_instance_v2` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        flow:
          type: string
        order:
          type: string
        ui_id:
          type: string
        parent_ui_id:
          type: string
        action_type:
          type: string
          description: Display value (label form).
      required: [sys_id]
      additionalProperties: true
    SysHubActionInstanceRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_hub_action_instance` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        flow:
          type: string
        order:
          type: string
        ui_id:
          type: string
        parent_ui_id:
          type: string
        action_type:
          type: string
          description: Display value (label form).
      required: [sys_id]
      additionalProperties: true
    SysHubTriggerInstanceRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_hub_trigger_instance` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        flow:
          type: string
        trigger_type:
          type: string
        table:
          type: string
        sys_class_name:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysHubTriggerInstanceV2Row:
      type: object
      description: 'Slim/indexed row shape from the `sys_hub_trigger_instance_v2` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        flow:
          type: string
        trigger_type:
          type: string
        sys_class_name:
          type: string
      required: [sys_id]
      additionalProperties: true
    SysHubFlowLogicRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_hub_flow_logic` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        flow:
          type: string
        order:
          type: string
        ui_id:
          type: string
        parent_ui_id:
          type: string
        logic_definition:
          type: string
          description: Display value (label form).
      required: [sys_id]
      additionalProperties: true
    SysSecurityAclRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_security_acl` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        operation:
          type: string
        type:
          type: string
          description: Display value (label form).
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        admin_overrides:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        decision_type:
          type: string
        sys_class_name:
          type: string
      required: [sys_id]
      additionalProperties: true
    SyseventInEmailActionRow:
      type: object
      description: 'Slim/indexed row shape from the `sysevent_in_email_action` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        type:
          type: string
        table:
          type: string
        action:
          type: string
        event_name:
          type: string
        order:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        stop_processing:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    SyseventEmailActionRow:
      type: object
      description: 'Slim/indexed row shape from the `sysevent_email_action` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        collection:
          type: string
        type:
          type: string
        event_name:
          type: string
        order:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        action_insert:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        action_update:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    SyseventRegisterRow:
      type: object
      description: 'Slim/indexed row shape from the `sysevent_register` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        event_name:
          type: string
        table:
          type: string
        sys_class_name:
          type: string
      required: [sys_id]
      additionalProperties: true
    SyseventScriptActionRow:
      type: object
      description: 'Slim/indexed row shape from the `sysevent_script_action` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        event_name:
          type: string
        order:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    SyseventEmailTemplateRow:
      type: object
      description: 'Slim/indexed row shape from the `sysevent_email_template` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        collection:
          type: string
      required: [sys_id]
      additionalProperties: true
    EmMatchRuleRow:
      type: object
      description: 'Slim/indexed row shape from the `em_match_rule` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        table:
          type: string
        ci_type:
          type: string
          description: Display value (label form).
        order:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    EmAlertCorrelationRuleRow:
      type: object
      description: 'Slim/indexed row shape from the `em_alert_correlation_rule` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        table:
          type: string
        relationship_type:
          type: string
          description: Display value (label form).
        order:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    EmAlertManagementRuleRow:
      type: object
      description: 'Slim/indexed row shape from the `em_alert_management_rule` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        type:
          type: string
          description: Display value (label form).
        order:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    EmImpactRuleRow:
      type: object
      description: 'Slim/indexed row shape from the `em_impact_rule` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        contribution_type:
          type: string
          description: Display value (label form).
      required: [sys_id]
      additionalProperties: true
    EmConnectorDefinitionRow:
      type: object
      description: 'Slim/indexed row shape from the `em_connector_definition` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        script_type:
          type: string
          description: Display value (label form).
      required: [sys_id]
      additionalProperties: true
    EmConnectorInstanceRow:
      type: object
      description: 'Slim/indexed row shape from the `em_connector_instance` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    SpPortalRow:
      type: object
      description: 'Slim/indexed row shape from the `sp_portal` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        url_suffix:
          type: string
        title:
          type: string
      required: [sys_id]
      additionalProperties: true
    SpPageRow:
      type: object
      description: 'Slim/indexed row shape from the `sp_page` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        id:
          type: string
        title:
          type: string
        internal:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        public:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    SpContainerRow:
      type: object
      description: 'Slim/indexed row shape from the `sp_container` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        title:
          type: string
        name:
          type: string
        order:
          type: string
      required: [sys_id]
      additionalProperties: true
    SpRowRow:
      type: object
      description: 'Slim/indexed row shape from the `sp_row` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        sp_column:
          type: string
        order:
          type: string
      required: [sys_id]
      additionalProperties: true
    SpColumnRow:
      type: object
      description: 'Slim/indexed row shape from the `sp_column` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        sp_row:
          type: string
        size:
          type: string
        order:
          type: string
      required: [sys_id]
      additionalProperties: true
    SpInstanceRow:
      type: object
      description: 'Slim/indexed row shape from the `sp_instance` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        sp_widget:
          type: string
        sp_column:
          type: string
        title:
          type: string
        order:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    SpWidgetRow:
      type: object
      description: 'Slim/indexed row shape from the `sp_widget` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        id:
          type: string
        name:
          type: string
        public:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    SysTemplateRow:
      type: object
      description: 'Slim/indexed row shape from the `sys_template` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        table:
          type: string
        short_description:
          type: string
        user:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
        global:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    CmdbCiOutageRow:
      type: object
      description: 'Slim/indexed row shape from the `cmdb_ci_outage` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        number:
          type: string
        cmdb_ci:
          type: string
        type:
          type: string
        begin:
          type: string
        end:
          type: string
        short_description:
          type: string
        u_outage_status:
          type: string
        task_number:
          type: string
      required: [sys_id]
      additionalProperties: true
    StdChangeRecordProducerRow:
      type: object
      description: 'Slim/indexed row shape from the `std_change_record_producer` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        short_description:
          type: string
        table_name:
          type: string
        category:
          type: string
        sys_class_name:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    ContractSlaRow:
      type: object
      description: 'Slim/indexed row shape from the `contract_sla` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        collection:
          type: string
        type:
          type: string
        target:
          type: string
        active:
          type: string
          enum: ['0', '1']
          description: ServiceNow boolean serialized as 0 or 1 in slim/indexed responses.
      required: [sys_id]
      additionalProperties: true
    AlmAssetRow:
      type: object
      description: 'Slim/indexed row shape from the `alm_asset` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        asset_tag:
          type: string
        display_name:
          type: string
        serial_number:
          type: string
        model:
          type: string
        model_category:
          type: string
        state:
          type: string
        substatus:
          type: string
        assigned_to:
          type: string
        owned_by:
          type: string
        location:
          type: string
        company:
          type: string
        cost_center:
          type: string
        sys_class_name:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
      required: [sys_id]
      additionalProperties: true
    AlmHardwareRow:
      type: object
      description: 'Slim/indexed row shape from the `alm_hardware` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        asset_tag:
          type: string
        display_name:
          type: string
        serial_number:
          type: string
        model:
          type: string
        model_category:
          type: string
        state:
          type: string
        substatus:
          type: string
        assigned_to:
          type: string
        owned_by:
          type: string
        location:
          type: string
        ci:
          type: string
        warranty_expiration:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
      required: [sys_id]
      additionalProperties: true
    SnEntFacilityAssetRow:
      type: object
      description: 'Slim/indexed row shape from the `sn_ent_facility_asset` table (cache-5min). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        asset_tag:
          type: string
        display_name:
          type: string
        serial_number:
          type: string
        model:
          type: string
        model_category:
          type: string
        state:
          type: string
        substatus:
          type: string
        assigned_to:
          type: string
        owned_by:
          type: string
        location:
          type: string
        ci:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
      required: [sys_id]
      additionalProperties: true
    AlmSoftwareLicenseRow:
      type: object
      description: 'Slim/indexed row shape from the `alm_software_license` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        asset_tag:
          type: string
        display_name:
          type: string
        vendor:
          type: string
        license_key:
          type: string
        license_count:
          type: string
        expiration_date:
          type: string
        state:
          type: string
        owned_by:
          type: string
        cost_center:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
      required: [sys_id]
      additionalProperties: true
    AlmConsumableRow:
      type: object
      description: 'Slim/indexed row shape from the `alm_consumable` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        asset_tag:
          type: string
        display_name:
          type: string
        quantity:
          type: string
        model:
          type: string
        location:
          type: string
        state:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
      required: [sys_id]
      additionalProperties: true
    AlmFacilityRow:
      type: object
      description: 'Slim/indexed row shape from the `alm_facility` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        asset_tag:
          type: string
        display_name:
          type: string
        location:
          type: string
        state:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
      required: [sys_id]
      additionalProperties: true
    AlmStockroomRow:
      type: object
      description: 'Slim/indexed row shape from the `alm_stockroom` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        display_name:
          type: string
        location:
          type: string
        manager:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
      required: [sys_id]
      additionalProperties: true
    AlmLicenseRow:
      type: object
      description: 'Slim/indexed row shape from the `alm_license` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        asset_tag:
          type: string
        display_name:
          type: string
        vendor:
          type: string
        license_count:
          type: string
        expiration_date:
          type: string
        state:
          type: string
        substatus:
          type: string
        owned_by:
          type: string
        cost_center:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
      required: [sys_id]
      additionalProperties: true
    CmdbCiSpkgRow:
      type: object
      description: 'Slim/indexed row shape from the `cmdb_ci_spkg` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        name:
          type: string
        version:
          type: string
        manufacturer:
          type: string
        vendor:
          type: string
        edition:
          type: string
        sys_class_name:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
      required: [sys_id]
      additionalProperties: true
    CmdbSoftwareInstanceRow:
      type: object
      description: 'Slim/indexed row shape from the `cmdb_software_instance` table (reference). In a non-slim response, a same-named raw field may instead retain its ServiceNow {value, display_value} envelope.'
      properties:
        sys_id:
          type: string
          description: ServiceNow primary key.
        ci:
          type: string
        software:
          type: string
        display_name:
          type: string
        version:
          type: string
        install_date:
          type: string
        install_status:
          type: string
        publisher:
          type: string
        sys_created_on:
          type: string
        sys_updated_on:
          type: string
      required: [sys_id]
      additionalProperties: true

x-filterable-columns:
  incident: [sys_id, number, short_description, state, active, priority, impact, urgency, assigned_to, assignment_group, cmdb_ci, caller_id, contact_type, opened_at, sys_created_on, sys_updated_on, legal_hold, sys_class_name, parent, category, subcategory]
  change_request: [sys_id, number, short_description, state, active, priority, impact, urgency, assigned_to, assignment_group, cmdb_ci, caller_id, contact_type, opened_at, sys_created_on, sys_updated_on, legal_hold, sys_class_name, parent, category, std_change_producer_version, chg_model, type]
  problem: [sys_id, number, short_description, state, active, priority, impact, urgency, assigned_to, assignment_group, cmdb_ci, caller_id, contact_type, opened_at, sys_created_on, sys_updated_on, legal_hold, sys_class_name, parent, category, subcategory]
  problem_task: [sys_id, number, short_description, state, active, priority, impact, urgency, assigned_to, assignment_group, cmdb_ci, caller_id, contact_type, opened_at, sys_created_on, sys_updated_on, legal_hold, sys_class_name, parent, problem]
  sc_request: [sys_id, number, short_description, state, active, priority, impact, urgency, assigned_to, assignment_group, cmdb_ci, caller_id, contact_type, opened_at, sys_created_on, sys_updated_on, legal_hold, sys_class_name, parent, requested_for]
  sc_req_item: [sys_id, number, short_description, state, active, priority, impact, urgency, assigned_to, assignment_group, cmdb_ci, caller_id, contact_type, opened_at, sys_created_on, sys_updated_on, legal_hold, sys_class_name, parent, requested_for, request, cat_item, configuration_item]
  sc_task: [sys_id, number, short_description, state, active, priority, impact, urgency, assigned_to, assignment_group, cmdb_ci, caller_id, contact_type, opened_at, sys_created_on, sys_updated_on, legal_hold, sys_class_name, parent, requested_for, request_item, request]
  incident_task: [sys_id, number, short_description, state, active, priority, impact, urgency, assigned_to, assignment_group, cmdb_ci, caller_id, contact_type, opened_at, sys_created_on, sys_updated_on, legal_hold, sys_class_name, parent, incident]
  change_task: [sys_id, number, short_description, state, active, priority, impact, urgency, assigned_to, assignment_group, cmdb_ci, caller_id, contact_type, opened_at, sys_created_on, sys_updated_on, legal_hold, sys_class_name, parent]
  sysapproval_group: [sys_id, number, short_description, state, active, priority, impact, urgency, assigned_to, assignment_group, cmdb_ci, caller_id, contact_type, opened_at, sys_created_on, sys_updated_on, legal_hold, sys_class_name, parent, approval, approval_user]
  asset_task: [sys_id, number, short_description, state, active, priority, impact, urgency, assigned_to, assignment_group, cmdb_ci, caller_id, contact_type, opened_at, sys_created_on, sys_updated_on, legal_hold, sys_class_name, parent]
  sn_contract_renewal_task: [sys_id, number, short_description, state, active, priority, impact, urgency, assigned_to, assignment_group, cmdb_ci, caller_id, contact_type, opened_at, sys_created_on, sys_updated_on, legal_hold, sys_class_name, parent]
  sys_user: [sys_id, user_name, name, email, title, department, location, active]
  sys_user_group: [sys_id, name, manager, description, active]
  sys_user_grmember: [sys_id, group, user]
  sys_user_delegate: [sys_id, user, delegate, starts, ends, approvals, assignments, notifications, invitations]
  sys_user_has_role: [sys_id, user, role, state, granted_by, inherited]
  sys_user_role: [sys_id, name, description]
  sys_group_has_role: [sys_id, group, role, granted_by, inherits]
  kb_knowledge: [sys_id, number, short_description, kb_knowledge_base, kb_category, workflow_state, author, article_type, valid_to, active]
  cmdb_ci: [sys_id, name, sys_class_name, operational_status, owned_by, short_description, install_status, discovery_source, last_discovered, support_group, category, ip_address, fqdn]
  cmdb_rel_ci: [sys_id, parent, child, type]
  sys_choice: [sys_id, name, table, element, value, label]
  core_company: [sys_id, name, short]
  cmn_department: [sys_id, name, cost_center]
  cmn_location: [sys_id, name, city, state]
  cmn_cost_center: [sys_id, code, name]
  sys_journal_field: [sys_id, name, element_id, element, value, sys_created_by, sys_created_on]
  sys_audit: [sys_id, tablename, documentkey, fieldname, fieldlabel, oldvalue, newvalue, user, sys_created_on]
  sys_attachment: [sys_id, table_name, table_sys_id, file_name, content_type, size_bytes, sys_created_by, sys_created_on]
  sys_email: [sys_id, instance, target_table, type, mailbox, state, subject, recipients, user, sys_created_on]
  task_ci: [sys_id, task, ci]
  task_sla: [sys_id, task, sla_definition, stage, business_percentage]
  sysapproval_approver: [sys_id, sysapproval, group, approver, state, sys_created_on]
  sc_cat_item: [sys_id, name, short_description, sys_class_name, sc_catalogs]
  item_option_new: [sys_id, name, question_text, type, cat_item, mandatory, order, reference]
  sc_item_option: [sys_id, value, item_option_new, cat_item, order]
  sc_item_option_mtom: [sys_id, request_item, sc_item_option]
  question: [sys_id, name, question_text, type]
  question_choice: [sys_id, question, text, value, order]
  sc_catalog: [sys_id, title, description, active]
  sc_category: [sys_id, title, description, sc_catalog, parent, active]
  catalog_ui_policy: [sys_id, cat_item, short_description, on_load, reverse_if_false, active, applies_to, order]
  catalog_ui_policy_action: [sys_id, ui_policy, cat_item, variable, mandatory, visible, disabled, cleared]
  catalog_script_client: [sys_id, cat_item, name, type, cat_variable, ui_type, active, applies_to]
  user_criteria: [sys_id, name, description, advanced, active]
  sc_cat_item_user_criteria_mtom: [sys_id, sc_cat_item, user_criteria]
  sc_cat_item_user_criteria_no_mtom: [sys_id, sc_cat_item, user_criteria]
  item_option_new_set: [sys_id, internal_name, title, description, layout, active]
  io_set_item: [sys_id, sc_cat_item, variable_set, order]
  topic: [sys_id, name, description, active]
  std_change_proposal: [sys_id, number, template_name, short_description, proposal_type, state, catalog, category, opened_at, sys_created_on, sys_updated_on]
  sys_script: [sys_id, name, collection, when, active, priority, condition, filter_condition]
  sys_script_client: [sys_id, name, table, type, ui_type, active, condition]
  sys_script_include: [sys_id, name, api_name, active, description]
  sysauto_script: [sys_id, name, active, run_type, run_period, run_time, run_dayofweek, run_dayofmonth, conditional, condition, run_as]
  sys_ui_policy: [sys_id, short_description, table, model_table, active, conditions, ui_type, run_scripts]
  sys_ui_policy_action: [sys_id, ui_policy, table, field, mandatory, visible, disabled]
  sys_data_policy2: [sys_id, short_description, model_table, active, conditions]
  sys_data_policy_rule: [sys_id, sys_data_policy, table, field, mandatory, disabled]
  sys_properties: [sys_id, name, value, type, description, sys_class_name]
  sys_ui_action: [sys_id, name, action_name, table, active, condition, client, form_button, list_button, order]
  sys_dictionary: [sys_id, name, element, internal_type, column_label, mandatory, default_value, reference, read_only, active]
  sys_dictionary_override: [sys_id, name, element, base_table, mandatory_override, read_only_override, default_value_override]
  sys_hub_flow: [sys_id, name, internal_name, active, type, sys_class_name, description]
  flow_inventory: [sys_id, name, internal_name, area, category, pattern, catalog_item, flow_type, trigger_table, run_count, error_count, last_run, created_by, curated, active, sys_updated_on]
  sys_hub_action_instance_v2: [sys_id, flow, order, ui_id, parent_ui_id, action_type]
  sys_hub_action_instance: [sys_id, flow, order, ui_id, parent_ui_id, action_type]
  sys_hub_trigger_instance: [sys_id, flow, trigger_type, table, sys_class_name]
  sys_hub_trigger_instance_v2: [sys_id, flow, trigger_type, sys_class_name]
  sys_hub_flow_logic: [sys_id, flow, order, ui_id, parent_ui_id, logic_definition]
  sys_security_acl: [sys_id, name, operation, type, active, admin_overrides, decision_type, sys_class_name]
  sysevent_in_email_action: [sys_id, name, type, table, action, event_name, order, active, stop_processing]
  sysevent_email_action: [sys_id, name, collection, type, event_name, order, active, action_insert, action_update]
  sysevent_register: [sys_id, event_name, table, sys_class_name]
  sysevent_script_action: [sys_id, name, event_name, order, active]
  sysevent_email_template: [sys_id, name, collection]
  em_match_rule: [sys_id, name, table, ci_type, order, active]
  em_alert_correlation_rule: [sys_id, name, table, relationship_type, order, active]
  em_alert_management_rule: [sys_id, name, type, order, active]
  em_impact_rule: [sys_id, name, contribution_type]
  em_connector_definition: [sys_id, name, script_type]
  em_connector_instance: [sys_id, name, active]
  sp_portal: [sys_id, url_suffix, title]
  sp_page: [sys_id, id, title, internal, public]
  sp_container: [sys_id, title, name, order]
  sp_row: [sys_id, sp_column, order]
  sp_column: [sys_id, sp_row, size, order]
  sp_instance: [sys_id, sp_widget, sp_column, title, order, active]
  sp_widget: [sys_id, id, name, public]
  sys_template: [sys_id, name, table, short_description, user, active, global]
  cmdb_ci_outage: [sys_id, number, cmdb_ci, type, begin, end, short_description, u_outage_status, task_number]
  std_change_record_producer: [sys_id, name, short_description, table_name, category, sys_class_name, active]
  contract_sla: [sys_id, name, collection, type, target, active]
  alm_asset: [sys_id, asset_tag, display_name, serial_number, model, model_category, state, substatus, assigned_to, owned_by, location, company, cost_center, sys_class_name, sys_created_on, sys_updated_on]
  alm_hardware: [sys_id, asset_tag, display_name, serial_number, model, model_category, state, substatus, assigned_to, owned_by, location, ci, warranty_expiration, sys_created_on, sys_updated_on]
  sn_ent_facility_asset: [sys_id, asset_tag, display_name, serial_number, model, model_category, state, substatus, assigned_to, owned_by, location, ci, sys_created_on, sys_updated_on]
  alm_software_license: [sys_id, asset_tag, display_name, vendor, license_key, license_count, expiration_date, state, owned_by, cost_center, sys_created_on, sys_updated_on]
  alm_consumable: [sys_id, asset_tag, display_name, quantity, model, location, state, sys_created_on, sys_updated_on]
  alm_facility: [sys_id, asset_tag, display_name, location, state, sys_created_on, sys_updated_on]
  alm_stockroom: [sys_id, name, display_name, location, manager, sys_created_on, sys_updated_on]
  alm_license: [sys_id, asset_tag, display_name, vendor, license_count, expiration_date, state, substatus, owned_by, cost_center, sys_created_on, sys_updated_on]
  cmdb_ci_spkg: [sys_id, name, version, manufacturer, vendor, edition, sys_class_name, sys_created_on, sys_updated_on]
  cmdb_software_instance: [sys_id, ci, software, display_name, version, install_date, install_status, publisher, sys_created_on, sys_updated_on]

x-task-tables:
  - asset_task
  - change_request
  - change_task
  - incident
  - incident_task
  - problem
  - problem_task
  - sc_req_item
  - sc_request
  - sc_task
  - sn_contract_renewal_task
  - sysapproval_group

x-cache-5min:
  - asset_task
  - catalog_script_client
  - catalog_ui_policy
  - catalog_ui_policy_action
  - change_request
  - change_task
  - cmn_cost_center
  - cmn_department
  - cmn_location
  - core_company
  - flow_inventory
  - incident_task
  - io_set_item
  - item_option_new
  - item_option_new_set
  - problem
  - problem_task
  - question
  - question_choice
  - sc_cat_item
  - sc_cat_item_user_criteria_mtom
  - sc_cat_item_user_criteria_no_mtom
  - sc_catalog
  - sc_category
  - sc_req_item
  - sc_request
  - sc_task
  - sn_contract_renewal_task
  - sn_ent_facility_asset
  - std_change_proposal
  - sys_choice
  - sys_data_policy2
  - sys_data_policy_rule
  - sys_dictionary
  - sys_dictionary_override
  - sys_hub_action_instance
  - sys_hub_action_instance_v2
  - sys_hub_flow
  - sys_hub_flow_logic
  - sys_hub_trigger_instance
  - sys_hub_trigger_instance_v2
  - sys_properties
  - sys_script
  - sys_script_client
  - sys_script_include
  - sys_security_acl
  - sys_ui_action
  - sys_ui_policy
  - sys_ui_policy_action
  - sys_user
  - sys_user_grmember
  - sys_user_group
  - sysapproval_approver
  - sysapproval_group
  - sysauto_script
  - task_ci
  - task_sla
  - topic
  - user_criteria

x-hr-parent-columns:
  incident_task: incident
  sys_attachment: table_sys_id
  sys_audit: documentkey
  sys_email: instance
  sys_journal_field: element_id
  sysapproval_approver: sysapproval
  sysapproval_group: parent
  task_ci: task
  task_sla: task
